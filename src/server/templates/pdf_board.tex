((* macro pdf_board(board, id, is_solution) *))
\pagestyle{empty}
\begin{center}
{\huge Eli Daian's Sudoku}

\begin{tikzpicture}[yscale=-1]
% Draw the table grid
\foreach \i in {0, ((( board.get_block_width() ))), ..., ((( board.get_block_width() * board.get_block_height() )))}
    \draw[very thick] (\i, 0) -- (\i, ((( board.get_block_width() * board.get_block_height() ))));
\foreach \i in {0, ((( board.get_block_height() ))), ..., ((( board.get_block_width() * board.get_block_height() )))}
    \draw[very thick] (0, \i) -- ( ((( board.get_block_width() * board.get_block_height() ))), \i);
\foreach \i in {0, ..., ((( board.get_block_width() - 1 )))} {
    ((*- for blocks_col in range(1, board.get_block_height()) *))
    \draw ( ((( board.get_block_height() ))) * \i + ((( blocks_col ))), 0) --
        ( ((( board.get_block_height() ))) * \i + ((( blocks_col ))), ((( board.get_block_width() * board.get_block_height() ))));
    ((*- endfor *))
};
\foreach \i in {0, ..., ((( board.get_block_height() -1 )))} {
    ((*- for blocks_row in range(1, board.get_block_width()) *))
    \draw (0, ((( board.get_block_width() ))) * \i + ((( blocks_row )))) --
        ( ((( board.get_block_width() * board.get_block_height() ))), ((( board.get_block_width() ))) * \i + ((( blocks_row ))));
    ((*- endfor *))
};

% Draw the cell data
((*- for row in range(board.get_block_width() * board.get_block_height()) *))
((*- for col in range(board.get_block_width() * board.get_block_height()) *))
\node at ( ((( row + 0.5 ))), ((( col + 0.5 ))) ) {\Large ((( board.get(row, col, is_solution) )))};
((*- endfor *))
((*- endfor *))
\end{tikzpicture}

\emph{\#((( id )))}
\end{center}
((* endmacro *))

\documentclass[a4paper, oneside]{article}
\usepackage[margin=2cm]{geometry}
\usepackage{tikz}

\begin{document}

((*- if multi_board *))
((*- for board, id in boards *))
((( pdf_board(board, id, is_solution) )))
\pagebreak
((*- endfor *))
((*- else *))
((( pdf_board(board, id, is_solution) )))
((*- endif *))

\end{document}
